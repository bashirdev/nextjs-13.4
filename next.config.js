/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'images.pixabay.com',
            'images.anotherwebsite.com',
        ]
    }
}

module.exports = nextConfig
